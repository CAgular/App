on:
  schedule:
    - cron: "*/30 * * * *"  # hver 30. minut
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -L --silent --show-error --fail https://knudsenhomeapp.streamlit.app/ > /dev/null
